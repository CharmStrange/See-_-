#include<stdio.h>
#include <stdlib.h>

#define ROW 9
#define COLUMN 9

unsigned char Universe[ROW][COLUMN];

unsigned char is_available() {
    unsigned char state;
    
    //for(unsigned char index = 0; index)
    
    return state;
}

int main() {
    if (ROW != COLUMN) {
        exit(1);
    }
}
